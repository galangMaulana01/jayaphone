"use client";
import{useCallback,useEffect,useState}from"react";import{Api,ApiError}from"@/lib/api";import type{DashboardStats,Transaksi}from"@/lib/types";import{CabangFilter}from"@/components/ui/CabangFilter";import{DateFilterBar}from"@/components/ui/DateFilterBar";import{EmptyState}from"@/components/ui/EmptyState";import{ErrorState}from"@/components/ui/ErrorState";import{LoadingSkeleton}from"@/components/ui/LoadingSkeleton";import{createDefaultDateFilter,toApiQueryParams,type DateFilterState}from"@/lib/utils/dateFilter";import{useAuth}from"@/contexts/AuthContext";import{formatDateTimeShort}from"@/lib/utils/formatters";
export default function LaporanPage():JSX.Element{const{user}=useAuth();const[filter,setFilter]=useState<DateFilterState>(createDefaultDateFilter());const[stats,setStats]=useState<DashboardStats|null>(null);const[items,setItems]=useState<Transaksi[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[branch,setBranch]=useState("");const load=useCallback(async()=>{setLoading(true);try{const params={...toApiQueryParams(filter),cabang:branch||undefined};const[s,t]=await Promise.all([Api.dashboard.stats(params),Api.transaksi.list({...params,limit:200})]);setStats(s.data);setItems(t.data||[])}catch(e){setError(e instanceof ApiError?e.message:"Gagal memuat laporan")}finally{setLoading(false)}},[filter,branch]);useEffect(()=>{void load()},[load]);const k=(stats as (DashboardStats&{keuangan?:DashboardStats["keuangan"]&{total_revenue?:number;total_modal?:number}})|null)?.keuangan;const revenue=k?.total_revenue??k?.total_omzet??0;const modal=k?.total_modal??0;const profit=k?.total_profit??0;const money=(v:number)=>(v||0).toLocaleString("id-ID");return <div className="jp-page"><div className="jp-page-header"><div><h1 className="jp-page-title">Laporan Keuangan</h1><p className="text-sm text-jp-muted dark:text-jp-muted-dark">{items.length} transaksi{user?.role==="kepala_cabang"?" · Cabang "+user.cabang:""}</p></div><div className="flex flex-col gap-2 sm:flex-row sm:items-start"><DateFilterBar currentFilterState={filter} onFilterStateChange={setFilter}/>{user?.role==="owner"&&<CabangFilter value={branch} onChange={setBranch} label="" className="min-w-[180px]"/>}</div></div>{loading?<LoadingSkeleton numberOfRows={6}/>:error?<ErrorState message={error} onRetry={load}/>:<><div className="grid grid-cols-1 gap-4 sm:grid-cols-[1.618fr_1fr_1fr]"><div className="metric-card"><p className="label">Total Revenue</p><p className="mt-1 jp-page-title">Rp {money(revenue)}</p></div><div className="metric-card"><p className="label">Total Modal</p><p className="mt-1 jp-page-title">Rp {money(modal)}</p></div><div className="metric-card"><p className="label">Gross Profit</p><p className="mt-1 jp-page-title font-mono text-jp-teal dark:text-jp-teal">Rp {money(profit)}</p></div></div><div className="table-wrap overflow-x-auto"><table className="w-full text-xs"><thead className="tbl-head"><tr>{["TRX ID","Unit","Kasir","Harga Jual","Modal","Profit","Waktu"].map(h=><th className="px-5 py-3 text-left" key={h}>{h}</th>)}</tr></thead><tbody>{items.length?items.map(t=><tr className="tbl-row" key={t.trx_id}><td className="px-5 py-4 font-mono text-jp-muted dark:text-jp-muted-dark">{t.trx_id}</td><td className="px-5 py-4">{t.unit_label}</td><td className="px-5 py-4">{t.kasir}</td><td className="whitespace-nowrap px-5 py-4 tabular-nums">Rp {money(t.harga_jual)}</td><td className="whitespace-nowrap px-5 py-4 tabular-nums">Rp {money(t.harga_modal)}</td><td className="whitespace-nowrap px-5 py-4 font-mono"><span className={t.profit<0?"text-jp-danger dark:text-jp-danger-dark":"text-jp-teal dark:text-jp-teal"}>Rp {money(t.profit)}</span>{t.profit<0&&<span className="badge badge-sold ml-2">Rugi</span>}</td><td className="whitespace-nowrap px-5 py-4 text-jp-muted dark:text-jp-muted-dark">{formatDateTimeShort(t.waktu)}</td></tr>):<tr><td colSpan={7}><EmptyState message="Belum ada transaksi" iconName="chartSvg"/></td></tr>}</tbody></table></div></>}</div>}
