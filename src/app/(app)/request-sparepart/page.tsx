"use client";
import { useMemo, useState } from "react";
import { Api } from "@/lib/api";
import { EmptyState } from "@/components/ui/EmptyState";
import { ErrorState } from "@/components/ui/ErrorState";
import { LoadingSkeleton } from "@/components/ui/LoadingSkeleton";
import { Modal } from "@/components/ui/Modal";
import { LabelledInput, LabelledSelect, LabelledTextarea } from "@/components/ui/InputField";
import { useAuth } from "@/contexts/AuthContext";
import { useToast } from "@/contexts/ToastContext";
import { useApiList } from "@/hooks/useApiList";
import { formatRupiah } from "@/lib/utils/formatters";
import type { RequestSparepart, Sparepart } from "@/lib/types";
export default function RequestSparepartPage(): JSX.Element {
  const { user } = useAuth(); const { showToast } = useToast(); const [status, setStatus] = useState(""); const [formOpen, setFormOpen] = useState(false); const [selected, setSelected] = useState<RequestSparepart | null>(null); const [type, setType] = useState("SPAREPART"); const [name, setName] = useState(""); const [quantity, setQuantity] = useState("1"); const [serviceId, setServiceId] = useState(""); const [spId, setSpId] = useState(""); const [productLink, setProductLink] = useState(""); const [note, setNote] = useState(""); const [estimate, setEstimate] = useState(""); const [stokQuery, setStokQuery] = useState("");
  // Backend only allows the assigned teknisi to create a request (app/routes/
  // request_sparepart.py buat_request: 403 for any other role) — kasir/
  // kepala_cabang used to see this button too and would always hit that 403.
  const canCreate = user?.role === "teknisi"; const canApprove = user?.role === "kepala_cabang";
  const { items, loading, error, reload: load } = useApiList<RequestSparepart>(() => Api.requestSparepart.list({ status: status || undefined }).then((r) => r.data ?? []), [status], "Gagal memuat request sparepart");
  // Teknisi checks branch stock right here, before requesting a part that
  // might already be on the shelf — a separate inventory page would just
  // duplicate this same GET /sparepart call, so it's folded into this page.
  const { items: stokItems, loading: stokLoading } = useApiList<Sparepart>(() => (canCreate ? Api.sparepart.list({}).then((r) => r.data ?? []) : Promise.resolve([])), [canCreate], "Gagal memuat stok sparepart");
  const visibleStok = useMemo(() => stokItems.filter((s) => `${s.nama} ${s.sp_id} ${s.kategori}`.toLowerCase().includes(stokQuery.toLowerCase())), [stokItems, stokQuery]);
  const openRequestForm = (prefill?: Sparepart) => { setType("SPAREPART"); setName(prefill?.nama ?? ""); setQuantity("1"); setServiceId(""); setSpId(prefill?.sp_id ?? ""); setProductLink(""); setNote(""); setFormOpen(true); };
  // Backend (app/services/request_sparepart_service.py create_request) always
  // requires service_id — it must reference a service ticket this teknisi is
  // currently handling (status Proses/Selesai, teknisi == actor) — and, when
  // sp_id is empty (a part not yet in the sparepart master), also requires an
  // https:// product_link. Neither was enforced or even collectable here
  // before, so a request for a new/uncatalogued part always failed with a
  // generic 400 the user had no way to act on.
  const create = async () => {
    if (!name.trim() || Number(quantity) <= 0) { showToast("Nama sparepart dan jumlah wajib diisi", "error"); return; }
    if (!serviceId.trim()) { showToast("Service ID wajib diisi — pilih tiket servis yang sedang Anda kerjakan", "error"); return; }
    if (!spId.trim() && !productLink.trim().startsWith("https://")) { showToast("Link produk (https://...) wajib diisi jika SP ID kosong", "error"); return; }
    try {
      await Api.requestSparepart.create({ tipe: type, service_id: serviceId.trim(), sp_id: spId || undefined, nama_sp: name.trim(), jumlah: Number(quantity), keterangan: note || undefined, cabang: user?.cabang || "JYP", product_link: productLink.trim() || undefined });
      showToast("Request sparepart berhasil dibuat"); setFormOpen(false); setName(""); setQuantity("1"); setServiceId(""); setSpId(""); setProductLink(""); setNote(""); await load();
    } catch (e) { showToast(e instanceof Error ? e.message : "Request gagal dibuat", "error"); }
  };
  // This page is the Kepala Cabang's INITIAL gate on a Pending request
  // (Diterima -> Menunggu_Kasir, or Ditolak) — that's the `respond`
  // endpoint's job (app/routes/request_sparepart.py `/{req_id}/respond`,
  // status enum "Diterima"/"Ditolak"). The kasir's separate FINAL approval
  // (setting harga_jual on a Menunggu_Kasir request) lives on its own page
  // (Approval Sparepart) and hits a different, kasir-only endpoint. This
  // component used to call that kasir-only endpoint here too (always 403
  // for kepala_cabang) and, on every path, used `selected.id` (the Mongo
  // _id) where the backend expects `req_id` (the business ID) — so neither
  // Setujui nor Tolak ever worked. Both are fixed below.
  const respond = async (decision: "Diterima" | "Ditolak") => { if (!selected) return; if (decision === "Ditolak" && !note.trim()) { showToast("Catatan wajib diisi saat menolak", "error"); return; } try { await Api.requestSparepart.respond(selected.req_id, { status: decision, estimasi_tiba: decision === "Diterima" ? (estimate || undefined) : undefined, catatan: note || undefined }); showToast(decision === "Diterima" ? "Request diterima, menunggu approval kasir" : "Request ditolak"); setSelected(null); setNote(""); await load(); } catch (e) { showToast(e instanceof Error ? e.message : "Aksi request gagal", "error"); } };
  return <div className="jp-page"><div className="jp-page-header"><div><h1 className="jp-page-title">Request Sparepart</h1><p className="text-sm text-jp-muted dark:text-jp-muted-dark">Permintaan sparepart untuk service dan stok</p></div>{canCreate && <button type="button" className="btn-primary" onClick={() => openRequestForm()}>+ Request Baru</button>}</div>{canCreate && <p className="rounded-jp-sm bg-jp-surface-subtle p-3 text-[11px] text-jp-muted dark:bg-jp-surface-subtle-dark/60 dark:text-jp-muted-dark">Alur: Request Anda (Pending) → direspon Kepala Cabang (Disetujui + estimasi tiba, atau Ditolak) → jika disetujui, menunggu penetapan harga jual final dari Kasir (Menunggu Kasir) → Selesai — sparepart otomatis masuk stok cabang dan biayanya ditambahkan ke modal unit terkait.</p>}{canCreate && <div className="space-y-2"><div className="flex items-center justify-between"><h2 className="text-sm font-semibold text-jp-text dark:text-jp-text-dark">Cek Stok Cabang</h2><input className="field-control w-full max-w-xs" placeholder="Cari sparepart..." value={stokQuery} onChange={(e) => setStokQuery(e.target.value)}/></div>{stokLoading ? <LoadingSkeleton numberOfRows={3}/> : <div className="table-wrap overflow-x-auto rounded-jp-md"><table className="w-full text-xs"><thead className="tbl-head border-b"><tr>{["ID","Nama","Kategori","Stok","Harga Jual","Aksi"].map((h) => <th key={h} className={`px-5 py-3.5 text-left font-medium ${h === "Aksi" ? "tbl-action-col" : ""}`}>{h}</th>)}</tr></thead><tbody>{visibleStok.length ? visibleStok.map((s) => <tr key={s.sp_id} className="tbl-row"><td className="px-5 py-4 font-mono text-jp-muted dark:text-jp-muted-dark">{s.sp_id}</td><td className="px-5 py-4 font-medium">{s.nama}</td><td className="px-5 py-4 text-jp-muted dark:text-jp-muted-dark">{s.kategori}</td><td className={`px-5 py-4 font-semibold ${s.stok <= 0 ? "text-jp-danger dark:text-jp-danger-dark" : "font-mono text-jp-text dark:text-jp-text-dark"}`}>{s.stok} {s.satuan}</td><td className="px-5 py-4">{formatRupiah(s.harga_jual)}</td><td className="tbl-action-col px-5 py-4"><button type="button" className="btn-ghost" onClick={() => openRequestForm(s)}>Request</button></td></tr>) : <tr><td colSpan={6}><EmptyState message="Belum ada sparepart di cabang Anda" iconName="wrenchSvg"/></td></tr>}</tbody></table></div>}</div>}<div className="segmented-control">{["","Pending","Disetujui","Ditolak","Selesai"].map((s) => <button type="button" key={s || "all"} className={`filter-tab ${status === s ? "filter-tab-active" : ""}`} onClick={() => setStatus(s)}>{s || "Semua"}</button>)}</div>{loading ? <LoadingSkeleton numberOfRows={5}/> : error ? <ErrorState message={error} onRetry={load}/> : <div className="table-wrap overflow-x-auto rounded-jp-md"><table className="w-full text-xs"><thead className="tbl-head border-b"><tr>{["Request","Sparepart","Tipe","Jumlah","Cabang","Status","Aksi"].map((h) => <th key={h} className={`px-5 py-3.5 text-left font-medium ${h === "Aksi" ? "tbl-action-col" : ""}`}>{h}</th>)}</tr></thead><tbody>{items.length ? items.map((r) => <tr key={r.id} className="tbl-row"><td className="px-5 py-4 font-mono text-jp-muted dark:text-jp-muted-dark">{r.req_id}</td><td className="px-5 py-4"><p className="font-medium">{r.nama_sp}</p><p className="text-[10px] text-jp-muted dark:text-jp-muted-dark">{r.sp_id || "Custom"}</p></td><td className="px-5 py-4 text-jp-muted dark:text-jp-muted-dark">{r.tipe}</td><td className="px-5 py-4">{r.jumlah}</td><td className="px-5 py-4 text-jp-muted dark:text-jp-muted-dark">{r.cabang}</td><td className="px-5 py-4"><span className="badge badge-booking">{r.status}</span></td><td className="tbl-action-col px-5 py-4">{canApprove && r.status === "Pending" ? <button type="button" className="btn-ghost" onClick={() => { setSelected(r); setEstimate(""); setNote(""); }}>Proses</button> : r.status === "Menunggu_Kasir" ? <span className="text-jp-muted dark:text-jp-muted-dark" title="Menunggu approval harga dari Kasir">Menunggu Kasir</span> : <span className="text-jp-muted dark:text-jp-muted-dark">—</span>}</td></tr>) : <tr><td colSpan={7}><EmptyState message="Belum ada request sparepart" iconName="packageSvg"/></td></tr>}</tbody></table></div>}
    <Modal isOpen={formOpen} onClose={() => setFormOpen(false)} title="Request Sparepart"><div className="space-y-3"><LabelledSelect label="Tipe" value={type} onChange={(e) => setType(e.target.value)}><option>SPAREPART</option><option>Servis</option><option>Barang</option></LabelledSelect><LabelledInput label="Nama Sparepart" required value={name} onChange={(e) => setName(e.target.value)}/><div className="grid grid-cols-2 gap-3"><LabelledInput label="SP ID (kosongkan jika belum ada di stok)" value={spId} onChange={(e) => setSpId(e.target.value)}/><LabelledInput label="Jumlah" type="number" min={1} required value={quantity} onChange={(e) => setQuantity(e.target.value)}/></div><LabelledInput label="Service ID" required helper="Tiket servis yang sedang Anda kerjakan (harus status Proses/Selesai)." value={serviceId} onChange={(e) => setServiceId(e.target.value)}/>{!spId.trim() && <LabelledInput label="Link Produk" required type="url" placeholder="https://..." helper="Wajib karena SP ID kosong — link referensi barang yang mau dibeli." value={productLink} onChange={(e) => setProductLink(e.target.value)}/>}<LabelledTextarea label="Keterangan" rows={3} value={note} onChange={(e) => setNote(e.target.value)}/><button type="button" className="btn-primary w-full" onClick={() => void create()}>Kirim Request</button></div></Modal>
    <Modal isOpen={Boolean(selected)} onClose={() => setSelected(null)} title={selected ? `Proses ${selected.req_id}` : "Proses Request"}><div className="space-y-3"><p className="text-sm text-jp-muted dark:text-jp-muted-dark">{selected?.nama_sp} · {selected?.jumlah} unit</p><p className="text-[11px] text-jp-muted dark:text-jp-muted-dark">Menerima akan meneruskan request ini ke Kasir untuk penetapan harga jual final.</p><LabelledInput label="Estimasi Tiba" type="date" value={estimate} onChange={(e) => setEstimate(e.target.value)}/><LabelledTextarea label="Catatan" rows={3} value={note} onChange={(e) => setNote(e.target.value)}/><div className="flex gap-2"><button type="button" className="btn-error flex-1" onClick={() => void respond("Ditolak")}>Tolak</button><button type="button" className="btn-success flex-1" onClick={() => void respond("Diterima")}>Setujui</button></div></div></Modal>
  </div>;
}
