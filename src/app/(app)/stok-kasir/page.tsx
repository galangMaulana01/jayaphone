"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import { Api, ApiError } from "@/lib/api";
import { EmptyState } from "@/components/ui/EmptyState";
import { ErrorState } from "@/components/ui/ErrorState";
import { LoadingSkeleton } from "@/components/ui/LoadingSkeleton";
import { Modal } from "@/components/ui/Modal";
import { UnitStatusBadge } from "@/components/ui/Badge";
import { useToast } from "@/contexts/ToastContext";
import { formatRupiah } from "@/lib/utils/formatters";
import type { Unit, UnitStatus } from "@/lib/types";

export default function StokKasirPage(): JSX.Element {
  const { showToast } = useToast(); const [items, setItems] = useState<Unit[]>([]); const [query, setQuery] = useState(""); const [selected, setSelected] = useState<Unit | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  const load = useCallback(async () => { setLoading(true); setError(""); try { setItems((await Api.units.list({ status: "Tersedia" })).data ?? []); } catch (e) { setError(e instanceof ApiError ? e.message : "Gagal memuat stok kasir"); } finally { setLoading(false); } }, []); useEffect(() => { void load(); }, [load]);
  const visible = useMemo(() => items.filter((u) => `${u.merk} ${u.tipe} ${u.unit_id} ${u.imei}`.toLowerCase().includes(query.toLowerCase())), [items, query]);
  const openDetail = async (id: string) => { try { setSelected((await Api.units.detail(id)).data); } catch (e) { showToast(e instanceof Error ? e.message : "Detail unit gagal", "error"); } };
  return <div className="space-y-5"><div><h1 className="text-3xl font-semibold tracking-[-0.03em] text-jp-text dark:text-jp-text-dark">Stok Kasir</h1><p className="text-sm text-jp-muted dark:text-jp-muted-dark">Unit tersedia untuk transaksi penjualan</p></div><input className="w-full max-w-lg rounded-xl border border-jp-border bg-jp-surface-subtle dark:border-jp-border-dark p-2 text-xs outline-none dark:bg-jp-surface-subtle-dark" placeholder="Cari merk, tipe, ID, IMEI..." value={query} onChange={(e) => setQuery(e.target.value)}/>{loading ? <LoadingSkeleton numberOfRows={5}/> : error ? <ErrorState message={error} onRetry={load}/> : <div className="table-wrap overflow-x-auto rounded-2xl"><table className="w-full text-xs"><thead className="tbl-head border-b"><tr>{["ID","Perangkat","Spesifikasi","IMEI","Harga Jual","Battery","Aksi"].map((h) => <th key={h} className="px-5 py-3.5 text-left font-medium">{h}</th>)}</tr></thead><tbody>{visible.length ? visible.map((u) => <tr key={u.unit_id} className="tbl-row"><td className="px-5 py-4 font-mono text-jp-muted dark:text-jp-muted-dark">{u.unit_id}</td><td className="px-5 py-4"><p className="font-medium">{u.merk}</p><p className="text-[10px] text-jp-muted dark:text-jp-muted-dark">{u.tipe}</p></td><td className="px-5 py-4 text-jp-muted dark:text-jp-muted-dark">{u.storage}/{u.ram} · {u.warna}<br/>{u.kondisi}</td><td className="px-5 py-4 font-mono text-jp-muted dark:text-jp-muted-dark">{u.imei}</td><td className="px-5 py-4 font-semibold font-mono text-jp-text dark:text-jp-text-dark">{formatRupiah(u.harga_jual)}</td><td className="px-5 py-4">{u.battery}%</td><td className="px-5 py-4"><button type="button" className="btn-ghost" onClick={() => void openDetail(u.unit_id)}>Detail</button></td></tr>) : <tr><td colSpan={7}><EmptyState message="Tidak ada unit tersedia" iconName="packageSvg"/></td></tr>}</tbody></table></div>}<Modal isOpen={Boolean(selected)} onClose={() => setSelected(null)} title={selected ? `Detail ${selected.unit_id}` : "Detail Unit"} maxWidthClassName="max-w-2xl">{selected && <div className="space-y-4 text-xs">{selected.foto_url && <img src={selected.foto_url} alt={selected.unit_id} className="h-56 w-full rounded-xl object-contain bg-jp-surface-subtle dark:bg-jp-surface-subtle-dark"/>}<div className="grid grid-cols-2 gap-3">{[["Status",<UnitStatusBadge key="status" status={selected.status as UnitStatus}/>],["Merk",selected.merk],["Tipe",selected.tipe],["Storage",selected.storage],["RAM",selected.ram],["Warna",selected.warna],["IMEI",selected.imei],["Battery",`${selected.battery}%`],["Harga Jual",formatRupiah(selected.harga_jual)]].map(([label,value]) => <div key={String(label)} className="rounded-xl bg-jp-surface-subtle p-3 dark:bg-jp-surface-subtle-dark/60"><p className="text-jp-muted dark:text-jp-muted-dark">{label}</p><div className="mt-1 font-medium">{value}</div></div>)}</div><button type="button" className="btn-ghost w-full" onClick={() => setSelected(null)}>Tutup</button></div>}</Modal></div>;
}
